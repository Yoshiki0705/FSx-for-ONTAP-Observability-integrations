# Pinned development and gate tooling.
#
# Exact pins, not ranges. A gate that resolves to ruff 0.15.20 locally and
# whatever is newest in CI returns different answers for identical code, and
# the disagreement surfaces as "works on my machine" rather than as a version
# problem. CI installs this file; the Makefile prefers .venv/bin over PATH so
# the pinned copies are the ones that actually run.
#
# Versions below match what is installed in .venv as of this pin, so adopting
# the file does not change local behaviour.

# --- Test runtime ---------------------------------------------------------
pytest==9.1.1
pytest-cov==7.1.0
coverage==7.14.3
moto==5.2.2
hypothesis==6.155.7

# --- Lambda runtime libraries (present in the AWS Python 3.12 runtime) -----
# urllib3 rather than requests: requests is not in the Lambda runtime.
boto3==1.43.36
urllib3==2.7.0

# --- Gates ----------------------------------------------------------------
# These three were absent from the venv entirely. bandit was not installed
# anywhere, so no Python security scan had ever run against this repo.
ruff==0.16.3
bandit==1.9.4
cfn-lint==1.55.1
